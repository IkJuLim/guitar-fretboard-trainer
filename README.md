# 🎸 Guitar Fretboard Trainer (기타 지판 암기 도우미)

기타 지판의 음 위치를 효율적으로 암기할 수 있도록 돕는 웹 기반 트레이닝 도구입니다.  
랜덤으로 음을 제시하되, **단순 무작위가 아닌 연습에 방해되는 패턴을 필터링**하여 더 효과적인 연습이 가능합니다.

> **[Live Demo 바로가기](https://IkJuLim.github.io/guitar-fretboard-trainer/)**

---

## 주요 기능 (Key Features)

### 1. 볼륨 컨트롤 (PASS_VOLUME)
- **양방향 동기화 UI:** 슬라이더를 움직이면 `PASS_VOLUME` 변수를 조절하여, 쓰레스홀드 값을 변경할 수 있습니다.

### 2. 랜덤 노트 생성
- **중복 방지:** 같은 음이 2번 연속으로 나오지 않습니다. (예: `5-E` -> `5-E` 방지)
- **특정 패턴 차단:** 연습 흐름을 끊거나 의도하지 않은 진행(예: `5-E` 직후 `6-E` 등장)을 사전에 차단하여 다시 추첨합니다.

---

## 기술 스택
- **Language:** HTML5, CSS3, JavaScript (Vanilla)
- **Deployment:** GitHub Pages

---

## 코드 핵심 로직

### 중복 및 패턴 필터링 로직
`while` 루프를 사용하여 조건에 맞지 않는 음이 나오면 즉시 재추첨합니다.

```javascript
while (!isValid) {
    // 1. 랜덤 추출
    currentNote = notePool[Math.floor(Math.random() * notePool.length)];

    // 2. 조건 검사
    if (currentNote === previousNote) {
        isValid = false; // 연속된 같은 음 방지
    } 
    else if (previousNote === '5-E' && currentNote === '6-E') {
        isValid = false; // 특정 패턴 방지 (5번줄 E -> 6번줄 E)
    } 
    else {
        isValid = true;
    }
}